# Diffusion-TS for Financial Risk Management

This Master's thesis project investigates the use of **Diffusion-TS**, a denoising diffusion probabilistic model for time series, to generate synthetic financial return sequences and compute portfolio risk measures, specifically **Value at Risk (VaR)** and **Expected Shortfall (ES)**. The diffusion-based approach is benchmarked against traditional risk estimation methods (Historical Simulation, Variance-Covariance, Monte Carlo) and a **DCC-GARCH** model. All results are validated through rigorous backtesting.

> **Note:** The Jupyter notebooks are designed to run on **Google Colab** with Google Drive integration.

---

## Table of Contents

- [Project Overview](#project-overview)
- [Folder Structure](#folder-structure)
  - [Root-Level Notebooks](#root-level-notebooks)
  - [Config/](#config)
  - [Data/](#data)
  - [Models/](#models)
  - [engine/](#engine)
  - [Utils/](#utils)
  - [grid_search/](#grid_search)
  - [Backtesting & GARCH/](#backtesting--garch)
- [Workflow](#workflow)
- [Portfolios](#portfolios)
- [Key Dependencies](#key-dependencies)

---

## Project Overview

The project follows a three-phase pipeline:

1. **Phase 1 – Model Training:** Train the Diffusion-TS model on historical return sequences using a grid search over hyperparameters.
2. **Phase 2 – Forecasting & Synthetic Generation:** Use the trained model to generate synthetic future return sequences for risk estimation.
3. **Phase 3 – Risk Calculation & Backtesting:** Compute VaR and ES from the generated sequences and benchmark them against traditional methods using formal backtesting procedures.

---

## Folder Structure

### Root-Level Notebooks

| Notebook | Description |
|---|---|
| `Diffusion-TS.ipynb` | **Phase 1** — Training of the base Diffusion-TS models. Contains the grid search over hyperparameters for both portfolios and evaluates generation quality using discriminative scores, predictive scores, cross-correlation loss, and PCA/t-SNE visualizations. |
| `Diffusion_TS_Forcasting.ipynb` | **Phase 2** — Forecasting experiments. Loads a trained checkpoint, generates synthetic return sequences for future time windows, and saves them as CSV files for downstream risk computation. |
| `VaR_ES_Calculation.ipynb` | **Phase 3a** — Computes VaR and ES using three traditional (baseline) methods: **Historical Simulation**, **Variance-Covariance**, and **Monte Carlo Simulation**. Uses a rolling window approach. |
| `VaR_ES_Calculation_Diffusion.ipynb` | **Phase 3b** — Computes VaR and ES from the **Diffusion-TS** generated synthetic sequences. Loads the forecasted sequences, reshapes them, and derives portfolio-level risk measures. |

---

### `Config/`

Contains **YAML configuration files** that define the full experiment setup for each portfolio.

| File | Description |
|---|---|
| `bluechip_sp500.yaml` | Configuration for the Blue-Chip (S&P 500) portfolio. |
| `crossAsset.yaml` | Configuration for the Cross-Asset portfolio. |


---

### `Data/`

Central data directory containing raw datasets, generated sequences, and computed risk measures.

| Subfolder / File | Description |
|---|---|
| `Datasets/` | Raw input CSV files with historical log-return series for both portfolios (`bluechip_sp500.csv`, `crossAsset_portfolio.csv`). Each file contains daily log-returns with one column per asset. |
| `Dataset_Code/` | Contains `Datasets.ipynb` — a notebook for data preparation, downloading, and preprocessing of the raw financial datasets. |
| `Sequences/` | Stores the **synthetic sequences** generated by the Diffusion-TS model. |
| `Sequences/blueChip/` | Generated sequences for the Blue-Chip portfolio. Contains `samples/` (sequence length 10) and `blueChip_34/` (the trained model checkpoint) subfolders with CSV files per forecast window. |
| `Sequences/crossAsset/` | Generated sequences for the Cross-Asset portfolio. Same structure as above. |
| `VaR_and_ES/` | Pre-computed VaR and ES results from all methods (Historical, Variance-Covariance, Monte Carlo, Diffusion) for both portfolios, stored as CSV files. |
| `build_dataloader.py` | Python module that builds PyTorch `DataLoader` objects from the configuration. Provides `build_dataloader()` for training data and `build_dataloader_cond()` for conditional generation / forecasting tasks. |

---

### `Models/`

Contains the neural network model implementations.

#### `Models/interpretable_diffusion/`

The core **Diffusion-TS** model implementation.

| File | Description |
|---|---|
| `gaussian_diffusion.py` | Main `Diffusion_TS` class; implements the full denoising diffusion probabilistic model. Includes the forward diffusion process (noise scheduling with linear/cosine beta schedules), the reverse denoising process (DDPM/DDIM sampling), and loss computation (L1/L2). |
| `transformer.py` | The **Transformer backbone** used as the denoising network. Features an interpretable architecture with specialized components: `TrendBlock` (polynomial trend modeling), `FourierLayer` (seasonality via inverse DFT), and `MovingBlock` (moving average decomposition), alongside standard multi-head self-attention with adaptive layer normalization. |
| `model_utils.py` | Utility functions for the model: positional encoding, normalization helpers (`normalize_to_neg_one_to_one`, `unnormalize_to_zero_to_one`), convolutional MLP blocks, adaptive layer norm, series decomposition, and tensor extraction helpers. |


| File | Description |
|---|---|
| `ts2vec.py` | Main TS2Vec class — trains a temporal contrastive encoder and provides `encode()` for extracting fixed-length representations of time series. |
| `utils.py` | Helper functions for TS2Vec (padding, splitting, centering). |
| `models/encoder.py` | The temporal encoder architecture. |
| `models/dilated_conv.py` | Dilated convolution layers used in the encoder. |
| `models/losses.py` | Hierarchical contrastive loss for TS2Vec training. |

---

### `engine/`

Training infrastructure and utilities.

| File | Description |
|---|---|
| `solver.py` | The **Trainer** class — orchestrates the full training loop. Handles model initialization, EMA (Exponential Moving Average) updates, gradient accumulation, checkpoint saving/loading, learning rate scheduling, and the training step logic. Also includes methods for training and loading a classifier for guided diffusion. |
| `logger.py` | **Logger** class — manages experiment logging. Saves configuration files, writes timestamped log entries to text files, and optionally integrates with TensorBoard for loss/metric tracking. |
| `lr_sch.py` | **ReduceLROnPlateauWithWarmup** — a custom learning rate scheduler that combines warmup (linear ramp-up) with reduce-on-plateau (halving LR when training loss stagnates). |

---

### `Utils/`

Utility modules for data processing, evaluation metrics, and visualization.

| File | Description |
|---|---|
| `io_utils.py` | I/O utilities — YAML/JSON config loading and saving, argument logging, random seed setting, config merging from command-line options, and dynamic class instantiation from config via `instantiate_from_config()`. |
| `metric_utils.py` | Evaluation and visualization utilities — PCA and t-SNE visualizations comparing real vs. synthetic data, train/test splitting, sequence length extraction, and confidence interval computation for metric scores. |
| `context_fid.py` | **Context-FID** computation — trains a TS2Vec encoder on real data, encodes both real and synthetic sequences, and computes the Fréchet Inception Distance (FID) in the learned representation space. |
| `cross_correlation.py` | **Cross-Correlation Loss** — computes the auto/cross-correlation function (CACF) of multivariate time series and measures the difference between real and synthetic correlation structures. |
| `discriminative_metric.py` | **Discriminative Score** — trains a post-hoc GRU-based classifier to distinguish real from synthetic sequences. The discriminative score is `|accuracy - 0.5|`; closer to 0 means better generation quality. Based on the TimeGAN evaluation framework. |
| `predictive_metric.py` | **Predictive Score** — trains a GRU on synthetic data to predict one-step-ahead values, then evaluates on real data using MAE. Lower scores indicate the synthetic data captures temporal dynamics well. Based on the TimeGAN evaluation framework. |
| `imputation_utils.py` | Imputation visualization and masked loss utilities — plots imputed values with uncertainty bands (quantiles) and provides a `MaskedLoss` class for computing MSE/L1 loss only on masked (predicted) positions. |
| `masking_utils.py` | Masking utilities — generates noise masks for data augmentation/imputation tasks with geometric or Bernoulli distributions. Also includes batch collation with padding and mask compensation for variable-length sequences. |

#### `Utils/Data_utils/`

Custom PyTorch `Dataset` classes for loading and preprocessing financial time series.

| File | Description |
|---|---|
| `real_datasets.py` | `CustomDataset` — general-purpose dataset class. Handles train/test splitting, sliding window extraction, normalization, and optional masking for imputation or prediction tasks. |

---

### `grid_search/`

Stores **training outputs from hyperparameter grid search** experiments. Each subfolder corresponds to a specific combination of hyperparameters.

```
grid_search/
├── blueChip/
│   ├── batch_size64_n_heads8_d_model128/    
│   │   ├── samples/                               ← Generated synthetic data
│   │   └── metrics_analysis.pdf                   ← Quality evaluation report
│   ├── batch_size128_n_heads4_d_model64/
│   ├── batch_size128_n_heads8_d_model128_34/      ← "_34" = seq_length 34
│   └── ...
└── crossAsset/
    ├── batch_size128_n_heads4_d_model128/
    └── ...
```

**Naming convention:** `batch_size{B}_n_heads{H}_d_model{D}[_34]`
- `B` — Batch size (64 or 128)
- `H` — Number of attention heads (4 or 8)
- `D` — Model dimension (64 or 128)
- `_34` — Sequence length of 34 

Each subfolder typically contains:
- Model checkpoints (`.pt` files)
- Configuration snapshots
- Generated samples and quality metrics (PDF reports)

---

### `Backtesting & GARCH/`

Contains **R scripts** for DCC-GARCH modeling and formal backtesting of all risk estimation methods.

| File | Description |
|---|---|
| `DCC_GARCH.R` | Fits a **Dynamic Conditional Correlation GARCH(1,1)** model with AR(1) mean specification using the `rmgarch` package. Performs rolling out-of-sample forecasts for portfolio VaR and ES. Outputs results as CSV files for backtesting. |
| `Backtesting.R` | Runs formal backtesting on VaR and ES estimates from **all five methods** (Historical, Variance-Covariance, Monte Carlo, GARCH, Diffusion). Uses the **ESR backtest** (`esback` package) for ES and **Christoffersen's conditional coverage test** (`quarks` package) for VaR. Results are evaluated over three yearly sub-periods and the full sample. |
| `Backtesting_Acerby.R` | Implements the **Acerbi & Székely (2014) Z2 test** for Expected Shortfall backtesting as an additional robustness check. |

#### `Backtesting & GARCH/Datasets/`

Portfolio return data used by the R scripts (`bluechip_portfolio.csv`, `crossAsset_portfolio.csv`).

#### `Backtesting & GARCH/VaR_and_ES/`

Backtesting results organized by significance level:

| Subfolder | Description |
|---|---|
| `var_95_es_97_5/` | Results for VaR at 95% and ES at 97.5% confidence level. |
| `var_97_5_es_97_5/` | Results for VaR at 97.5% and ES at 97.5% confidence level. Used for the Z2 Test.
| `Plots/` | Visualization plots for each method and portfolio (e.g., `blueChip_Diffusion.png`, `crossAsset_GARCH.png`). |

---

## Workflow

```
┌─────────────────────────────────────────────────────────────────┐
│  1. DATA PREPARATION                                            │
│     Data/Dataset_Code/Datasets.ipynb → Data/Datasets/*.csv      │
└──────────────────────────┬──────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│  2. MODEL TRAINING (Phase 1)                                     │
│     Diffusion-TS.ipynb + Config/*.yaml                           │
│     → grid_search/{portfolio}/... (checkpoints + metrics)        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│  3. FORECASTING (Phase 2)                                        │
│     Diffusion_TS_Forcasting.ipynb                                │
│     → Data/Sequences/{portfolio}/samples/*.csv                   │
└──────────────┬───────────────────────────┬──────────────────────┘
               │                           │
┌──────────────▼───────────┐ ┌─────────────▼──────────────────────┐
│  4a. TRADITIONAL VaR/ES  │ │  4b. DIFFUSION-BASED VaR/ES        │
│  VaR_ES_Calculation.ipynb│ │  VaR_ES_Calculation_Diffusion.ipynb │
│  → Data/VaR_and_ES/*.csv │ │  → Data/VaR_and_ES/*.csv           │
└──────────────┬───────────┘ └─────────────┬──────────────────────┘
               │                           │
┌──────────────▼───────────┐               │
│  4c. DCC-GARCH VaR/ES    │               │
│  DCC_GARCH.R             │               │
│  → VaR_and_ES/*.csv      │               │
└──────────────┬───────────┘               │
               │                           │
┌──────────────▼───────────────────────────▼──────────────────────┐
│  5. BACKTESTING (Phase 3)                                        │
│     Backtesting.R + Backtesting_Acerby.R                         │
│     → VaR_and_ES/Plots/*.png + Test_Results.pdf                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Portfolios

| Portfolio | Assets | Description |
|---|---|---|
| **Blue-Chip (S&P 500)** | 5 | Large-cap U.S. equities from the S&P 500 index. Training window: 4,564 observations. |
| **Cross-Asset** | 5 | Diversified portfolio across multiple asset classes. Training window: 5,298 observations. |

---

## Key Dependencies

### Python
- PyTorch, ema-pytorch
- NumPy, Pandas, SciPy, scikit-learn
- Matplotlib, Seaborn
- GluonTS
- tqdm, PyYAML
- einops

### R
- `rugarch`, `rmgarch` (GARCH modeling)
- `quarks` (VaR coverage tests)
- `esback` (ES backtesting)
- `readr`, `dplyr`, `gridExtra`
